# health-checkup-risk-report-ai
End-to-end pipeline that preprocesses health-checkup data, predicts risk with LightGBM + SHAP, and auto-generates explainable natural-language reports — no external LLM API required.
